# Qwen Code package

The runtime provider is `acp:qwen` and the signing key ID is `tutti-qwen-release-v1`. Keep all package content declarative.

`icon.svg` is the transparent conversation-row mask glyph. The colored
`sidebar-icon.svg` is the shared branded identity used in the Provider Rail,
conversation headers, Message Center, and mentions.
