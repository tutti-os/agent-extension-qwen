# Agent instructions

Use the `tutti-agent-extension` Skill and keep the package declarative. Do not commit secrets, runtime binaries, generated archives, or `node_modules`. Validate the real ACP runtime, run repository checks, use Conventional Commits, and add DCO sign-off.
